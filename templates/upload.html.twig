{% extends "navigation.bar.html.twig" %}

    {% block head %}
        {{ parent() }}
    {% endblock %}
{% block content %}
    <h2>Upload file</h2>
    {{ form_start(form) }}
    {{ form_row(form.product) }}
    <button class="btn btn-lg btn-primary" type="submit">
        Upload
    </button>
    {{ form_end(form) }}
{% endblock %}